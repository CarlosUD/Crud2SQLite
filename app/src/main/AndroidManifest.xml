<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itca.Crud2SQLite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CRUDV2">
        <activity
            android:name="com.itca.Crud2SQLite.detallesarticulos"
            android:exported="true" />
        <activity
            android:name="com.itca.Crud2SQLite.list_view_articulos"
            android:exported="true" />
        <activity
            android:name="com.itca.Crud2SQLite.ConsultaSpinner"
            android:exported="true" />
        <activity
            android:name="com.itca.Crud2SQLite.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="screenSize|orientation"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/Theme.CRUDV2.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>